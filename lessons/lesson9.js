// Импортируем класс CustomerDetails из модуля "../helpers/printHelper.js"
import { customerDetails } from "../helpers/printHelper.js";

// Теперь, после импорта, можно создать его экземпляр


// Вызываем метод printFirstName у экземпляра класса
customerDetails.printFirstName("Steve");
customerDetails.printLastName('Smith')